(load "std.limo")

(when argv
      (progn (write (list "running" (car argv)))
      	     (load (car argv))
	     (setf argv (car argv))
	     (exit)))

(load "tests.limo")
(write "*** end of limo.init ****")
